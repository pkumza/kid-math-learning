# 关卡生成指南（给 AI 和人）

本指南是 `AGENTS.md` 的细化版，提供可复用模板和自检办法。

## 一、标准关卡骨架

每关建议包含以下部分：

1. `intro`: 一句话引入，告诉孩子这关学什么。
2. `explore`: 演示一个典型场景，强调“看懂方法”而非算快。
3. `practice`: `3~6` 题，单点训练，难度从易到中。
4. `challenge`: `1~2` 题，组合推理，保留成就感。
5. `done`: 结尾徽章与鼓励语。

## 二、探索和闯关如何“不要太像”

可使用以下策略做区分：

- 探索用“演示型”语境（讲解、拆步、观察），闯关用“任务型”语境（选择、判断、应用）。
- 探索强调方法词（先、再、最后），闯关强调目标词（求、选、判断）。
- 探索示例只给 1 个完整案例，闯关至少 3 个变式题。
- 不要把探索题的句式原样复制到闯关题，只替换数字。

## 三、题量与难度分配

- `practice`: `3~6` 题，推荐 `4~5` 题。
- `challenge`: `1~2` 题，推荐 `2` 题（其中 1 题可标困难）。
- 跨关节奏：每 `6` 关安排 `1` 关“困难关”。

建议难度曲线：

- 第 1~3 题：直接应用
- 第 4~5 题：轻微变式
- 挑战题：两步推理或跨表示切换（文字 <-> 图形）

## 四、提示（💡）规范

- 每题必须有 `hint` 文案。
- 页面默认不显示提示。
- 只有点击 `💡` 后才展示该题提示。
- 提示应是“方法提示”，避免直接给最终答案。

提示文案模板：

- “先固定 ___，再看 ___ 的变化。”
- “把题目拆成两步：先算 ___，再算 ___。”
- “先画/列出所有可能，再去重。”

## 五、可直接复用的出题模板

### 基础题模板（practice）

- 模板 A：单步计算  
  例：给定条件 -> 直接求结果。
- 模板 B：判断正误  
  例：给出说法 -> 选“对/错”或选择正确项。
- 模板 C：生活应用  
  例：时间、购物、分配、路线中做一步决策。

### 挑战题模板（challenge）

- 模板 D：两步组合  
  先求中间量，再求最终量。
- 模板 E：逆向推理  
  给结果反推条件。

## 六、关卡生成 Checklist（提交前必过）

- [ ] `practice` 在 `3~6` 题范围内
- [ ] `challenge` 在 `1~2` 题范围内
- [ ] 每题都有 `hint`
- [ ] 提示默认隐藏，仅点 `💡` 展示
- [ ] 探索与闯关题面明显区分
- [ ] 若处于 6 关节奏点，已标记并设计困难关
- [ ] `index.html` 入口已补充且分组正确

## 七、给 AI 的建议提示词（可复制）

你正在为本仓库新增一个小学数学关卡。请严格遵守：

1. `practice` 生成 4~5 题（允许 3~6）。
2. `challenge` 生成 1~2 题。
3. 每题必须有 `hint`，并实现“点击 `💡` 才显示提示”。
4. 探索区与闯关区必须使用不同语境和句式，禁止仅改数字复用题面。
5. 如果该关是每 6 关中的困难关，请在挑战题中体现两步推理，但不超纲。
6. 修改后输出：新增文件、`index.html` 改动、规则自检结果。
