# 自检记录

检查日期：2026-09-08。

## 仓库规则逐条核对

- [x] 实施前读取 `AGENTS.md` 与 `LEVEL_DESIGN_RULES.md`，先给出关卡草案，再按确认的完整计划实现。
- [x] 每关都有 `explore`、`practice`、`challenge`，另有介绍与完成反馈。
- [x] 每关基础题 4 道，符合 3～6 道限制；共 24 道。
- [x] 前五关各 1 道挑战，第六关 2 道，符合 1～2 道限制；共 7 道。
- [x] 六个探索和全部 31 道题都有 `hint`。提示默认隐藏，点击 💡 后才显示；换题重新隐藏。
- [x] 答错仅提示重试，不自动展开、朗读数学提示，不给出正确答案。
- [x] 探索采用自由搬运、平衡、机器配置、库存增减、上下车、按清单装篮；正式题另设目标和线索，不复用探索句子换数字。
- [x] 第 6 关标记困难；挑战包含连续代换或“变化—均分—再增加”，提示分步展开。
- [x] 基础题以一个主要数量关系为主，挑战组合推理；不要求掌握新的运算符号。
- [x] 选择题的每个干扰项在配置中有解释；答案唯一。31 道题逐题独立核对。
- [x] 全部文案是中文短句，故事按 1～4 张卡展开。问题可单独重听。
- [x] 数字键盘、主要动作、阶段切换与提示按钮最小高度 56px；支持鼠标、键盘与触控点击，不要求拖拽。
- [x] 正确和错误均有即时明确反馈；答对后才显示验证解法，由孩子主动进入下一题。
- [x] 页面初次进入不自动朗读，不读取隐藏提示；切换阶段或离开页面取消上一段语音。
- [x] 六个关卡使用中文主题名 HTML，共享配置沿用 `q`、`hint`、`visual`、`answer`、`options` 等既有字段名。
- [x] 新目录 `forest-math/` 独立组织；原 `index.html` 只增加新系列入口，所有原有关卡入口及顺序保留。
- [x] 进度使用独立存储键；完成条件检查探索与全部正式题，重复答题或重玩不重复发贴纸。
- [x] 没有倒计时、生命值、排行榜、默认展开提示或 3 道以上的单关挑战。

## 自动核验

`node forest-math/verify.cjs`：**43 项通过**。

包含：固定预期答案与算式树两路核算、每题结构与可输入范围、选项唯一性和干扰项依据、0～200 数量图、完成条件、刷新恢复、损坏和越界存档、存储不可用、文件模式跳转与进度合并、旧片段不覆盖新位置、语音降级、静态资源存在，以及旧首页入口保留。

四个产品脚本均通过 `node --check`；`git diff --check` 通过。游戏没有构建依赖或外部资源加载。

## 浏览器核验

通过本地 HTTP 服务，在 Codex 内置浏览器中完成：

- 6 关全部 31 道题及每关通关流程；首页最终正确显示 6 / 6 张贴纸。
- 六种探索操作：搬运、天平达到平衡、机器换规则、送来与拿走、上下车、两个篮子满足清单。
- 实物答题：选备货盘、连续放入、拿回一个、确认。
- 数字答题：输入两三位数、删除、修改、确认。
- 选择题：先选错误答案，获得不泄露解法的反馈，再改选正确答案。
- 默认提示隐藏；答错后仍隐藏；主动逐层展开困难题提示。
- 逐句信息卡保留、单张重听、从头重听后答案仍保留；新题不预填答案。
- 刷新恢复当前题位置；重玩回到基础题；已完成贴纸不重复累计。
- WebMCP 两个接口真实注册；读取不返回隐藏线索或答案；合法提交走同一套反馈与进度；未读完故事或非法输入会被拒绝。
- 820×1180、1180×820、390×844 视口下，文档宽度等于视口宽度，没有横向溢出。困难题信息卡分别排列为两列、四列、单列；数字键盘实测每键高度 56px。
- 正常宽度首页截图检查通过；末次关卡控制台错误日志为空。

## 验证范围与限制

- 浏览器工具的 URL 安全策略禁止自动访问 `file://`，所以没有绕过限制尝试其他浏览器。直接双击打开的兼容性通过资源、普通脚本加载和文件模式存储逻辑检查，**未作浏览器实测**。HTTP 运行方式已完整实测。
- 平板和手机采用浏览器视口模拟，未使用实体 iPad 或手机测试。
- 下方早期语音记录已由全量本地配音替换。当前不使用系统朗读；测试覆盖调用、取消、静音和文件不可用处理。
- 本地 HTTP 试玩数据仅在测试浏览器的对应地址下。换浏览器或首次打开本地文件时，使用独立进度。

### 2026-09-09 拖拽改进

- 5 道备货盘题改为单物品双向拖拽，操作目标 56 × 56 px；轻点和键盘仍可完成操作。
- 正向、反向、盘外、同盘、取消、多指、失焦、轻点、防重复计数、上下界、提交锁定：独立事件回归通过。
- 原 43 项核验通过；未新增关卡或变更题面、题量、提示、困难关及入口。
- 本次没有完成浏览器实际拖拽验证：预览连接失败后工具拦截了错误页，未绕过限制。事件回归使用模拟命中区域，不等同于真实触屏测试。

### 2026-09-09 前两关本地配音

- Kokoro-82M-v1.1-zh 本机合成，默认 zf_001 / speed 0.9。完整生成使用 HF_HUB_OFFLINE=1，未调用付费 API。
- 共 64 段正式配音，覆盖前两关现有全部 say() 文本；总时长 293 秒，含两段试听的 audio 目录约 2.2 MB。
- 64 段均通过 ffprobe 格式/时长检查及 ffmpeg 完整解码；均为单声道 MP3。
- 44 项功能核验及拖拽事件核验通过。新增测试覆盖本地配音优先、重听中止旧音频、静音、迟到错误隔离。
- 配音清单仅加载于兔兔菜摊和松鼠仓库；其余四关不变。提示仍默认隐藏、点击后播放。
- 两种声音的试听页已提供。声音自然程度请以实际试听为准；未声称完成真人听审或真实平板音频播放测试。

### 2026-09-09 全部六关配音扩展

- 六关均加载声音 A 清单，共 210 段，合计 946 秒；MP3 格式、时长与完整解码检查通过。
- 所有玩法、故事卡、问题和点开的提示均有录音；缺少文件时保留文字并提示，不再调用系统声音。
- 44 项功能核验、拖拽事件核验通过；本次为本地文件与模拟音频接口检查，没有新增真机听审结论。
- 原有关卡、题量、提示时机、答案、进度与入口保持原样。

### 答对与通关语音反馈

- 新增 31 段逐题成功说明与 6 段整关庆祝录音，全部沿用声音 A；正式配音清单现共 247 段。
- 只在答案核验成功后播放，挑战题先说“挑战成功”；答对反馈可重听。既有答案、题量、提示时机不变。
- `scripts/test-success-audio.cjs` 在 jsdom 中验证全部 31 题与 6 次通关，覆盖答错隔离、自动播放、重听、静音和下一题中止。
- 44 项原有核验、拖拽回归及全部 247 段音频解码检查通过。修正原核验脚本对已提交森林入口的重复过滤比较问题。
- 播放接口使用模拟 Audio；没有声称完成真实平板听审。
