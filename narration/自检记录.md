# 全部关卡配音替换自检（2026-09-09）

- 范围：主仓库全部 80 关和首页欢迎语；森林六关另有独立清单。
- 声音：用户确认的 A，Kokoro-82M-v1.1-zh / zf_001 / speed 0.9。本机离线合成，无付费 API。
- 主仓库产物：2082 段完整句子、3482 段短语与数字，共 5564 个 MP3；合计 15605 秒、113,087,700 字节。
- 森林产物：210 段正式配音，合计 946 秒，六关全部接入。
- 音频核验：所有正式 MP3 均通过格式、时长和完整解码检查。主仓库全部为 24 kHz 单声道；口读文本与生成规则一致。短语只裁掉首尾静音，保留内部停顿。
- 算式核验：乘号、除号不被拼音清理误删；分数按分母分之分子读；字母序列逐个读；129 段受早期清理规则影响的句子已重新生成。
- 操作覆盖：jsdom 执行全部 80 关，检查 3554 个朗读场景；包括正误选项、点击提示、动画结束、分数涂色、数字组成、钟表、金额、周期等。每个调用均匹配实际音频文件。
- 数字核验：0–99999 全范围可匹配录音；较大数的补零、小数、分数、金额均有回归检查。
- 播放核验：首页欢迎语、重听替换、静音、切换阶段、离开页面、旧回调隔离和连续片段中止通过。动态片段沿用首次点击启动的音频元素，并预载后续文件；没有系统朗读回退。
- 关卡保护：前 50 关与 Git HEAD 的 AST、页面标记对比通过，只有 say/show 的朗读接入及脚本引用变化。所有 80 个既有入口保留。未新增关卡，未改变题量、阶段或提示展示时机。
- 原有修正保留：第 79 关 120−20=100；第 69 关 19×4 估为 20×4=80。
- 其他核验：423 个主页面脚本块和修改后的共享 JS 通过解析；森林 44 项功能核验、拖拽事件核验、git diff --check 通过。
- 验证边界：播放接口使用模拟 Audio，音频使用 ffprobe/ffmpeg 实际解码。未逐段人工听审，也未新增真实平板播放测试。动态短语之间可能有轻微停顿。

分发时保留完整 narration 与 forest-math/audio 目录；播放不需要 Python、模型或 API 密钥。
