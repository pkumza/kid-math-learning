# AGENTS: 新关卡生成硬规则

本文件用于约束 AI 在本仓库生成新关卡时的行为。若与临时指令冲突，以用户最新指令为准。

## 1) 关卡结构硬约束

- 每关必须包含：探索（explore）+ 基础闯关（practice）+ 挑战（challenge）。
- `practice` 题量必须为 `3~6` 题。
- `challenge` 题量必须为 `1~2` 题。
- 每关必须提供 `💡` 提示能力，且提示默认不展示，只有点击后展示。
- 探索与闯关题面不能高度同构，禁止“同一句话只换数字”。
- 文案面向小学生，短句、口语化、低负担。

## 2) 节奏硬约束（跨关）

- 按关卡顺序，每 `6` 关至少安排 `1` 个困难关。
- 困难关应体现在挑战题推理链更长，不应依赖超纲知识。
- 困难关要有更清晰提示，避免挫败感。

## 3) 题目设计规则

- 基础题优先覆盖单一知识点，挑战题可做两步组合。
- 错误选项要“有迷惑性但可解释”，不能随机凑数。
- 每题必须包含简短 `hint`，提示仅点按后可见。
- 题目语境尽量生活化（时间、购物、分配、路线、图形）。

## 4) 交互与可用性

- 可点击目标足够大，适配平板触控。
- 成功/失败反馈要即时且明确。
- 语音文案避免长句，尽量一口气可读完。
- 默认不自动泄露答案，不在未点击提示时暴露关键解法。

## 5) 文件与命名

- 新关卡文件名使用中文主题名，如 `年龄问题.html`。
- 在 `index.html` 中新增入口时，保持模块语义与排序一致。
- 如果使用配置驱动结构，字段命名保持与现有关卡一致。

## 6) AI 生成流程（必须执行）

1. 先读取本文件与 `LEVEL_DESIGN_RULES.md`。
2. 先给出关卡草案：主题、学习目标、题量分配、难度位次。
3. 再产出代码与入口修改。
4. 最后输出自检清单，逐条确认本文件规则是否满足。

## 7) 禁止项

- 不得删除既有关卡入口。
- 不得把探索区直接当成闯关题复用。
- 不得省略 `hint` 或默认展开 `hint`。
- 不得把挑战题数量扩展到 3 题及以上。
